# 首页视觉阶段 1 验收

唯一视觉参考：用户提供的 `codex-clipboard-0dba4f48-2817-457d-80a4-99b6cf0e34b2.png`。

| 设计稿区域 | 实现组件 | 验收点 |
| --- | --- | --- |
| 顶部导航 | `PixelNavbar` | 七层原创公会纹章、六项导航、搜索、用户入口、移动菜单 |
| Hero 场景 | `GuildHero` | 开源像素天空、远山、森林、建筑和角色素材分层组合；叠加自制光照、景深、部门道具、纹章、标题与双按钮 |
| RPG 状态栏 | `GuildStats` | 等级/经验/荣誉/成立年份读取站点设置；82 名活跃成员与 328 条完成活动由 SQLite 聚合 |
| 三大入口 | `AdventureCard` | 开源像素背景与 CSS/SVG 前景组合为历史书卷、职业旗帜、活动相机三套入口插画 |
| 公会公告 | `NoticeBoard` | 读取公开公告表，支持日期、置顶 NEW 标识、站内跳转、加载/错误/空状态与吉祥物提示 |
| 页脚 | `PixelFooter` | 快速入口、社交入口、在线成员、版权信息 |

## 动画与交互

- SVG 云层与 CSS 云朵分别缓慢移动。
- 独立环境景深层包含暖光束、左右薄雾与十枚错时漂浮光粒，增强空间层次。
- 火把、蜡烛和职业大厅火焰逐帧闪烁。
- 四名角色按不同延迟轻微待机浮动。
- 入口卡悬浮抬升，按钮具有 hover 与 active 位移反馈。
- 页面进入执行淡入动画；减少动画偏好沿用全局 `prefers-reduced-motion` 规则。

## 素材边界

- 未使用 AI 生成图片。
- 未把设计稿或相似背景图直接铺设为背景。
- 首页场景与角色使用许可证已登记的开源像素素材，并通过独立图层组合；纹章、光照、部门道具和界面装饰由 SVG/CSS 绘制。
- 中文标题与界面文字使用 OFL 许可的 Fusion Pixel Font，不再依赖普通系统中文字体。

## 数据闭环

- `GET /api/public/home` 一次返回数据库聚合状态和公开公告，不再使用组件内硬编码数组。
- 管理员可在“公会公告”中新增、编辑、置顶、发布与下架；普通成员和部门负责人不可维护全站公告。
- 开发种子以关联项目的匿名规模为参考，生成 82 名虚构成员、328 条完成活动和四条虚构公告；不导入姓名、照片或个人答卷。
- 旧开发数据库启动时会幂等补齐本阶段模拟数据，生产现有库不会被开发种子回填。

## 产品设计层级

- 首页只有 Hero 标题一个 `primary` 主视觉焦点，建筑招牌与纹章作为支持层。
- 标题、按钮和关键栏目使用像素字体；正文、公告和辅助说明改用高可读中文 UI 字体。
- HUD、入口卡、公告板统一使用深木、古铜、暖金材质变量和一致的高光/投影规则。
- 768px 平板端保持三张核心入口及三栏页脚并列；640px 以下才切换为纵向移动布局。
- 所有首页链接与按钮提供清晰的键盘 `focus-visible` 状态。
